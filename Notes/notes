-- Роботы собираются в кучу
-- Пример р1 близко к р2 и спереди. Заносим в соар. Список триад (р* где относит р*)
от инпут линк дерево. 12 пар для 4 роботов. каждого
-- Правила: если какой то робот далеко двигаться к нему. К самому дальнему. Каждый двигается к самому дальнему. Правила: если далеко от нас то делаем шаг. Оператор: шаг к роботу которому. Варианты шагов к каким-то роботам. Несколько вариантов -- рандомно выполняются, равенство ставим. 
-- Выполнение в аутпут линк название робота к которому двигаться
-- Шаг например раз в секунду или идем пока не придем к нему или каждый шаг принимаем решение
-- Когда предлагает новый оператор, срабатывает несколько правил, соар выбирает один, случайно. Соар ввод вывод

ВОПРОСЫ
-- Когда происходит смещение робота? 
    1 случай: у одного робота решение и он его выполняет и так до какого момента? нужно обновлять соотношения соседей, когда?
    2 случай: группа получает решение для каждого робота и выбирает, что выполнять
    3 случай: в группе последовательно каждый робот получает решение, выполняет движение, обновляется карта и переходит к следующему роботу
    
